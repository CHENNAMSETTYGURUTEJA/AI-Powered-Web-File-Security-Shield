services:
  - type: web
    name: ai-powered-web-file-security-shield
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:$PORT app:app
    envVars:
      - key: PORT
        value: 10000
    rootDir: backend
